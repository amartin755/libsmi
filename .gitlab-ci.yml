image: gcc

before_script:
  - apt-get update -qq
  - apt-get install -y -qq make gcc flex bison autoconf automake pkg-config libtool libtool-bin m4
 
stages:
  - build
  - test

build:
  stage: build
  script:
    - ./autogen.sh
    - make

test:
  stage: test
  script:
    - make check
